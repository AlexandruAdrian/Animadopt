import registerFormStyles from './RegisterFormStyles';

const style = {
  ...registerFormStyles,
  form: {
    '& > button': {
      marginTop: '40px',
      width: '100%',
    },
  },
};

export default style;
