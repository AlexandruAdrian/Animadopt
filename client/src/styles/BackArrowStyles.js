import colors from './colors';

const styles = {
  backArrow: {
    position: 'absolute',
    top: '15px',
    left: '7px',
    textDecoration: 'none',
    color: colors.darkPacificBlue,
  },
};

export default styles;
