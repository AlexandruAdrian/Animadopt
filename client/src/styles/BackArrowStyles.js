import colors from './colors';

const styles = {
  backArrow: {
    position: 'absolute',
    top: '13px',
    left: '7px',
    textDecoration: 'none',
    color: colors.pacificBlue,
  },
};

export default styles;
